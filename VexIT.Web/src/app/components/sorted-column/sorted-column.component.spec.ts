/*
import {async, ComponentFixture, TestBed} from '@angular/core/testing';

import {SortedColumnComponent} from './sorted-column.component';
import {ActivatedRoute, Router} from '@angular/router';
import {ActivatedRouterStub} from '../../tests/activated-router-stub';
import {RouterStub} from '../../tests/router-stub';
import {AuthService} from '../../services/auth.service';
import {JwtHelperService} from '@auth0/angular-jwt';
import {HttpClient, HttpHandler} from '@angular/common/http';
import {FormsModule, ReactiveFormsModule} from '@angular/forms';
import {ComponentCoreService} from '../../services/component-core.service';
import {AppConfigService} from '../../services/app-config.service';

describe('SortedColumnComponent', () => {
  let component: SortedColumnComponent;
  let fixture: ComponentFixture<SortedColumnComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      imports: [ReactiveFormsModule, FormsModule],
      providers: [HttpClient, HttpHandler, ComponentCoreService, AuthService, JwtHelperService, AppConfigService,
        {provide: Router, useClass: RouterStub},
        {provide: ActivatedRoute, useValue: ActivatedRouterStub.Value}],
      declarations: [SortedColumnComponent]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SortedColumnComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
*/
